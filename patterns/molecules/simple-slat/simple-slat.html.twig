{#
/**
 * @file
 * Simple Slat pattern.
 */
#}
{% set variants = variants|merge({'is_slat': true}) %}
{%
  set vars = {
    'context': context,
    'image': image,
    'link': link,
    'description': description,
    'tag': tag,
    'title': title
   }
%}
{{ pattern('simple_block', vars) }}
